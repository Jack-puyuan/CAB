<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/wel"
    tools:context=".WelcomeActivity">

    <Button
        android:id="@+id/btn_welcome_customer"
        android:layout_width="250dp"
        android:layout_height="60dp"
        android:layout_marginTop="44dp"
        android:background="@color/colorAccent"
        android:onClick="goToCustomer"
        android:text="I'm a customer"
        android:textSize="18sp"
        app:layout_constraintEnd_toEndOf="@+id/btn_welcome_driver"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="@+id/btn_welcome_driver"
        app:layout_constraintTop_toBottomOf="@+id/btn_welcome_driver" />

    <Button
        android:id="@+id/btn_welcome_driver"
        android:layout_width="250dp"
        android:layout_height="60dp"
        android:layout_marginBottom="232dp"
        android:background="@android:color/background_light"
        android:onClick="goToDriver"
        android:text="I'm a driver"
        android:textSize="18sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.544"
        app:layout_constraintStart_toStartOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>